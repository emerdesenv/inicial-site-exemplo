name: Segurança - Detecção de Segredos (Gitleaks)

on:
  push:
  pull_request:

jobs:
  secrets-scan:
    name: Verificar segredos no código
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Rodar Gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          fail: true